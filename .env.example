# ── LLM Configuration ────────────────────────────────
LLM_RESPONSES_URL=http://127.0.0.1:8318/v1/chat/completions
LLM_API_KEY=sk-12345678
LLM_MODEL_NAME=gpt-5.1-codex-mini
LLM_REASONING_EFFORT=none

# ── Simulation Parameters ────────────────────────────
MAX_AGENTS=100
MAX_ROUNDS=40
MAX_BRANCHES=8
MEMORY_COMPRESS_INTERVAL=5
BRANCH_PRUNE_THRESHOLD=0.05
FORK_SENSITIVITY=0.7
# DEFAULT_NUM_AGENTS: Default agent count when user doesn't specify (range 3-100)
DEFAULT_NUM_AGENTS=20
# DEFAULT_ROUNDS: Default simulation rounds when user doesn't specify
DEFAULT_ROUNDS=10

# ── Concurrency ──────────────────────────────────────
LLM_CONCURRENCY=5

# ── Database ─────────────────────────────────────────
DATABASE_URL=sqlite:///./swarmoracle.db
CHROMA_PERSIST_DIR=./chroma_data

# ── Server ───────────────────────────────────────────
HOST=0.0.0.0
PORT=18927
CORS_ORIGINS=["http://localhost:5173","http://localhost:9528","http://localhost:18928"]
